name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0

  firebase_auth: ^4.19.4
  firebase_core: ^2.30.1
  flutterfire_ui: ^0.4.3+20 # Update to the latest version available
  cloud_firestore: # Update to the latest version available
  intl: # Update to the latest version available

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/2.jpeg
    - assets/dedo_image.png
    - assets/pepo_image.png
